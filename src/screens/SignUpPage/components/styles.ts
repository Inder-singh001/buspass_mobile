import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    padding: 20,
  },
  title: {
    fontSize: 30,
    marginBottom: 2,
  },
  text: {
    fontSize: 16,
    paddingBottom: 8,
  },
  input: {
    width: '100%',
    borderColor: '#ccc',
    borderRadius: 5,
    overflow: 'hidden',
    alignItems:'center',
    marginBottom: 10,
    color: '#00000080',
    fontSize: 12,
  },
  submit:
  {
    
  }
});
